applyMultiplatform()

kotlin {
    sourceSets {
        commonMain {
            dependencies {
                api project(':rd-core')
            }
        }
        jvmTest {
            kotlin {
                dependencies {

                }
                srcDirs += ["${parent.buildDir}/models/interning"]
                srcDirs += ["${parent.buildDir}/models/demo"]
            }
        }
    }
}

compileTestKotlinJvm.dependsOn(':rd-gen:generateEverything')
