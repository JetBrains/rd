applyMultiplatform()

kotlin {
    sourceSets {
        commonMain {
            dependencies {
                api project(':rd-kt:rd-core')
            }
        }
        jvmTest {
            kotlin {
                dependencies {
//                    compile project(':rd-kt:rd-gen')
                }
                srcDirs += ["${rootProject.buildDir}/models/interning"]
                srcDirs += ["${rootProject.buildDir}/models/demo"]
            }
        }
    }
}

compileTestKotlinJvm.dependsOn(':rd-kt:rd-gen:generateEverything')
